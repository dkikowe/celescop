# 🐛 Отладка загрузки фотографий с телефона

## Проблема
При загрузке фото с телефона (JPEG или сделанный снимок) выходит "неизвестная ошибка" в Telegram WebApp.

## Что было исправлено

### 1. ❌ Проблема с Content-Type
**Было:** Вручную устанавливался заголовок `'Content-Type': 'multipart/form-data'`  
**Проблема:** При ручной установке этого заголовка не добавляется `boundary`, который необходим для правильной обработки FormData  
**Решение:** Удален ручной заголовок - браузер сам установит правильный `Content-Type` с `boundary`

**Файлы:**
- ✅ `src/services/user.service.ts` - метод `editUserPhoto`
- ✅ `src/services/goal.service.ts` - метод `completeGoal`

### 2. ❌ Проблема с обработкой ошибок
**Было:** Axios interceptor не возвращал `Promise.reject(error)`  
**Проблема:** Ошибки "проглатывались" и реальное сообщение с сервера не доходило до пользователя  
**Решение:** Добавлен `return Promise.reject(error)` в axios interceptor

**Файл:** `src/lib/axios.ts`

### 3. ✅ Добавлено подробное логирование
Теперь в консоли браузера видно:
- 📤 Какой запрос отправляется (метод, URL)
- 📁 Информация о файле (имя, размер, тип)
- 🔴 Детали ошибки (статус, данные ответа, полная ошибка)

## Как отладить проблему на телефоне

### Шаг 1: Открыть консоль разработчика
На Android/iOS в Telegram WebApp можно открыть консоль несколькими способами:

#### Вариант А: Desktop Telegram
1. Откройте Telegram Desktop на компьютере
2. Откройте WebApp
3. Нажмите `Ctrl+Shift+I` (Windows/Linux) или `Cmd+Option+I` (Mac)
4. Перейдите на вкладку Console

#### Вариант Б: Chrome Remote Debugging (Android)
1. Включите "Режим разработчика" на Android
2. Подключите телефон к компьютеру
3. Откройте Chrome на компьютере: `chrome://inspect`
4. Откройте WebApp в Telegram на телефоне
5. Нажмите "inspect" в Chrome

#### Вариант В: Safari Web Inspector (iOS)
1. Включите "Веб-инспектор" в настройках Safari на iPhone
2. Подключите iPhone к Mac
3. Откройте Safari на Mac → Develop → [Ваш iPhone]
4. Откройте WebApp в Telegram на iPhone

### Шаг 2: Попробовать загрузить фото
Попробуйте загрузить фото и смотрите на консоль. Вы увидите:

```
📤 POST /goal/123/complete
  image: {name: "IMG_1234.jpg", size: 2451234, type: "image/jpeg"}
```

Если произойдет ошибка:
```
🔴 API Error
URL: https://api.example.com/goal/123/complete
Method: post
Status: 400
Response data: {message: "Файл слишком большой"}
```

### Шаг 3: Проверить, что показывается пользователю
После ошибки проверьте toast уведомление. Теперь должно показываться:
- ✅ Конкретное сообщение от сервера (например, "Файл слишком большой")
- ✅ Статус код для серверных ошибок (например, "Ошибка сервера (500): ...")
- ✅ Информативное сообщение для сетевых ошибок

## Возможные причины ошибок

### 1. Размер файла > 5MB
**Ошибка:** "Файл слишком большой. Максимальный размер: 5MB"  
**Решение:** Попросите пользователя выбрать фото меньшего размера или сжать его

### 2. Неподдерживаемый формат
**Ошибка:** "Неподдерживаемый формат. Используйте JPG, PNG или HEIC"  
**Решение:** Проверьте, что формат файла в списке: `['image/jpeg', 'image/jpg', 'image/png', 'image/heic', 'image/heif']`

### 3. Проблемы на сервере
**Ошибка:** "Ошибка сервера (500): ..."  
**Решение:** Проверьте логи сервера, возможно проблема с обработкой multipart/form-data

### 4. Проблемы с сетью
**Ошибка:** "Ошибка сети. Проверьте интернет-соединение."  
**Решение:** Проверьте интернет-соединение на телефоне

### 5. Проблемы с авторизацией
**Ошибка:** Автоматически попытается обновить токен через `/auth/refresh`  
**Решение:** Если обновление токена не помогло, попросите пользователя перезапустить приложение

## Проверочный список

- [ ] В консоли видны логи `📤 POST ...` при загрузке?
- [ ] В логах видна информация о файле (name, size, type)?
- [ ] Если есть ошибка, в консоли есть блок `🔴 API Error`?
- [ ] Какой статус код в ошибке?
- [ ] Что в `Response data`?
- [ ] Какое сообщение показано пользователю в toast?

## Следующие шаги

Если проблема сохраняется:

1. **Сделайте скриншот консоли** с полным логом ошибки
2. **Проверьте логи сервера** - возможно проблема на бэкенде
3. **Проверьте настройки CORS** на сервере
4. **Проверьте лимиты** на размер загружаемых файлов на сервере (nginx, node.js и т.д.)
5. **Попробуйте разные форматы** - JPEG, PNG, HEIC
6. **Попробуйте разные размеры** - маленький файл, средний, большой

## Дополнительная информация

### Формат запроса
```
POST /goal/{id}/complete
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary...
Authorization: Bearer {token}

------WebKitFormBoundary...
Content-Disposition: form-data; name="image"; filename="photo.jpg"
Content-Type: image/jpeg

[binary data]
------WebKitFormBoundary...--
```

### Ожидаемые заголовки ответа от сервера
```json
{
  "success": true,
  "data": { ... }
}
```

Или в случае ошибки:
```json
{
  "message": "Описание ошибки",
  "error": "Код ошибки" // опционально
}
```

